Commit 4 — feat: draft initial Transaction table schema

File to add:
  src/database/models.py

What to do:
  1. Add this draft model. It's deliberately rough — pulled straight
     from a first pass of the Stripe/Salesforce API docs.
  2. Don't worry about nullability, indexes, or exact types yet —
     that correction happens at commit 6.

Commit message:
  feat: draft initial Transaction table schema
