Commit 2 — chore: add environment variable template

File to add:
  .env.example

What to do:
  1. Copy .env.example into the repo root.
  2. cp .env.example .env, then fill .env with real local values (never commit .env).

Commit message:
  chore: add environment variable template
