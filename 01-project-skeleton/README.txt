Commit 1 — chore: initial project setup

Files to add:
  requirements.txt
  .gitignore

What to do:
  1. Copy these two files into the repo root.
  2. python -m venv venv && source venv/bin/activate
  3. pip install -r requirements.txt

Commit message:
  chore: initial project setup
